#include "BST.h"

#include "analysis.h"

int main() {
    /*int keys[] = {13, 4, 31, 7, 93, 12, 1, 34, 25, 5, 6, 96, 20, 30};
    int size = 14;

    BST bst(keys, size);
    bst.findFullBTLevel();
    bst.lowestCommonAncestor(1, 68);
    bst.lowestCommonAncestor(5, 25);
    bst.maximumSumPath();
    bst.maximumWidth();
    bst.keyLevel(7);
    bst.insertKey(15);
    bst.insertKey(34);
    bst.deleteKey(5);
    bst.deleteKey(0);
    bst.displayInorder();
    bst.displayPostOrder();*/

    analysis a{};
    a.timeAnalysis();


    return 0;
}
